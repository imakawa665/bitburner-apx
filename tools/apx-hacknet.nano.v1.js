export async function main(ns){ ns.disableLog('sleep'); const F=ns.flags([['budget',0.2],['maxROI',3600],['log','false']]); function reserve(){ try{ return Math.max(0, Number(ns.read('reserve.txt')||0)); }catch{return 0;} } function free(){ return Math.max(0, ns.getServerMoneyAvailable('home')-reserve()); } while(true){ let changed=false; const costNew=ns.hacknet.getPurchaseNodeCost(); if(costNew>0 && costNew <= free()*Number(F.budget||0.2)){ ns.hacknet.purchaseNode(); changed=true; } for(let i=0;i<ns.hacknet.numNodes();i++){ const can=(fn)=>{ const c=fn(i); return c>0 && c<= free()*Number(F.budget||0.2); }; if(can(ns.hacknet.getLevelUpgradeCost)) ns.hacknet.upgradeLevel(i,1); if(can(ns.hacknet.getRamUpgradeCost)) ns.hacknet.upgradeRam(i,1); if(can(ns.hacknet.getCoreUpgradeCost)) ns.hacknet.upgradeCore(i,1); } if(!changed) await ns.sleep(1500); } }